"""spec-020: platform authentication -- Google sign-in for humans, JWT
sessions, layered alongside (not replacing) spec-017's shared API key,
which narrows to machine-to-machine paths (webhook callbacks) that can't
perform a login flow."""
